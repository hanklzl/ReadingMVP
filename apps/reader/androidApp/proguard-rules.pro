# Project-specific release keep rules.
#
# The Android Gradle Plugin, Compose, and Kotlin libraries provide their own
# consumer rules. Keep this file small and add only rules backed by release
# build failures or runtime evidence.
